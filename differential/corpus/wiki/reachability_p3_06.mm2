;; @desc   MORK.wiki "MM2-tutorial:-Reachability-P3", code block 6

(exec 0 (, (startpoint $p))
        (, (path $p $p 0)))

(exec 1 (, (path $p $q $d) (street $q $r $e) 
           (maxlength $max) (($d + $e) <= $max))
        (, (path $p $r ($d + $e))))
